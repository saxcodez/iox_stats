# Text for the GitHub repository page

Paste these into the repository's **About** box (gear icon on the repository page, top right).

**Description** (max. 350 characters):

> macOS system stats, done like Apple would: live menu bar readout, iOS-style glass widgets and a native widget
> gallery widget - CPU, memory, disk, network, ping and the CPU temperature macOS hides. Non-commercial
> community project; ideas (AI usage meter, time to home) and contributions welcome.

**Website:** leave empty, or later the releases page.

**Topics:** `macos` `menu-bar` `widgets` `widgetkit` `swiftui` `system-monitor` `apple-silicon` `python` `pyside6`
`cpu-temperature` `community`

**Social preview** (Settings > General > Social preview): use `docs/screenshots/dashboard-dark.png`.
